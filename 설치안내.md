# 유포리아투어 관리자 대쉬보드 — 설치 안내

한 번만 설정하면 그 뒤로는 **대쉬보드에서 고치고 저장 버튼만 누르면** 사이트가 바뀝니다.
GitHub에 파일을 올리실 필요가 없어집니다.

---

## 1. 올릴 파일

압축을 풀면 아래 구조입니다. 그대로 저장소에 올려 주세요.

```
admin.html                          ← 대쉬보드 (새 파일)
concert.html                        ← 공연 목록이 보이도록 수정됨
data/concerts.json                  ← 공연 일정 (새 파일, 빈 목록)
data/fares.json                     ← 교통 요금 (새 파일, 빈 목록)
data/costsheets.json                ← 원가표 (새 파일, 빈 목록)
netlify/functions/admin-api.js      ← 관리자 백엔드 (새 파일)
```

`netlify/functions/` 폴더가 저장소에 이미 있으니 그 안에 `admin-api.js`만 넣으시면 됩니다.

---

## 2. GitHub 토큰 만들기 (한 번만)

대쉬보드가 저장소에 커밋하려면 열쇠가 필요합니다.

1. GitHub 오른쪽 위 프로필 → **Settings**
2. 맨 아래 **Developer settings** → **Personal access tokens** → **Fine-grained tokens**
3. **Generate new token** 을 누르고 이렇게 설정합니다
   - **Token name** — `euforia-admin`
   - **Expiration** — 1년 (만료되면 다시 만들어야 합니다)
   - **Repository access** — *Only select repositories* → `ornate-chebakia-a4180f` 하나만
   - **Permissions** → *Repository permissions* → **Contents** 를 **Read and write** 로
4. **Generate token** → 화면에 나오는 긴 문자열을 복사합니다

> 이 문자열은 **그 화면을 벗어나면 다시 볼 수 없습니다.** 바로 다음 단계에 붙여 넣으세요.
> 저에게도, 다른 누구에게도 보내지 마세요.

---

## 3. Netlify 환경변수 넣기 (한 번만)

Netlify → 해당 사이트 → **Site configuration** → **Environment variables** → **Add a variable**

| 이름 | 값 |
|---|---|
| `ADMIN_PASSWORD` | 대쉬보드에 쓰실 비밀번호 (직접 정하세요) |
| `ADMIN_SECRET` | 아무 긴 문자열 (아래 참고) |
| `GITHUB_TOKEN` | 2단계에서 복사한 토큰 |
| `GITHUB_REPO` | `euforia-jpg/ornate-chebakia-a4180f` |
| `GITHUB_BRANCH` | `main` |
| `NETLIFY_BUILD_HOOK` | (선택) 아래 4번 참고 |

**ADMIN_SECRET 만드는 법** — 로그인 상태를 확인하는 데 쓰는 값이라 아무도 못 맞출 문자열이면 됩니다.
키보드를 마구 눌러 40자 정도 만드셔도 되고, 아래를 그대로 쓰셔도 됩니다(한 번 정하면 바꾸지 마세요).

```
euf-2026-x7Kq9mZp3Rt8vNbW5jHs2Lc4Yd6Fg1Aa
```

넣고 나면 **Deploys → Trigger deploy → Deploy site** 로 한 번 배포해야 환경변수가 적용됩니다.

---

## 4. 재배포 버튼 (선택)

대쉬보드의 "지금 다시 배포하기" 버튼을 쓰시려면 빌드 훅이 필요합니다.
없어도 저장은 정상 작동합니다 (저장하면 자동으로 배포됩니다).

Netlify → **Site configuration** → **Build & deploy** → **Build hooks** → **Add build hook**
이름은 `admin-manual`, 브랜치는 `main` → 생성된 주소를 `NETLIFY_BUILD_HOOK` 에 넣으세요.

---

## 5. 접속

```
https://toureplus.co.kr/admin
```

메뉴에 없는 주소입니다. 검색엔진에도 잡히지 않게 막아 두었습니다.
북마크해 두고 쓰시면 됩니다.

---

## 쓰는 법

### 여행상품
상품을 누르면 편집 화면이 열립니다. 요금·기간·태그·포함/불포함·상세 일정표까지 고칠 수 있습니다.
**한 줄에 하나씩** 이라고 적힌 칸(태그, 하이라이트, 포함 사항 등)은 엔터로 줄을 나누면 됩니다.

### 공연 일정
`+ 공연 추가` 로 줄을 만들고 날짜·도시·공연장·공연명을 채웁니다.
**날짜가 지난 공연은 사이트에서 자동으로 사라집니다.** 지우실 필요 없습니다.
등록된 공연이 하나도 없으면 공연 페이지는 지금처럼 "준비중" 안내가 나옵니다.

### 교통 요금
MOVE 요금표입니다. 아직 요금을 안 정하셨으니 비워 두셔도 됩니다.

### 원가표 · 마진
단체별 원가를 넣으면 1인당 원가와 마진별 판매가가 바로 나옵니다.

- **상품을 고르면** 홈페이지에 올라간 판매가를 가져와 **현재 마진율**을 계산합니다
- **팀당 / 1인당** 구분이 핵심입니다
  - 팀당 — 인원과 상관없이 한 팀에 드는 비용 (차량 · 기사 · 가이드 · 로컬가이드 · 기사숙식 · 가이드숙식 · 가이드교통비)
  - 1인당 — 사람 수만큼 늘어나는 비용 (식사 · 입장료 · 호텔 · 시티택스)
- 수량은 **일수 · 박수를 바꾸면 자동으로 따라옵니다.** 직접 고친 칸은 그대로 둡니다
- 마진 계산은 두 가지 중에 고르실 수 있습니다
  - **매출 기준** (권장) — 판매가 = 원가 ÷ (1 − 마진). 정산할 때 쓰는 방식입니다
  - **원가 기준** — 판매가 = 원가 × (1 + 마진)
  - 같은 "30%"라도 결과가 다릅니다. 원가 100유로면 매출 기준은 142.9유로, 원가 기준은 130유로입니다
- 마진이 **0% 아래면 적자 경고**, **10% 아래면 얇다는 경고**가 뜹니다
- 원가표는 단체명으로 저장해 두면 나중에 그대로 불러옵니다.
  저장 버튼을 눌러 목록에 담은 뒤 아래 **저장하고 배포**까지 눌러야 실제로 남습니다

### 배포 · 이력
누가 언제 무엇을 고쳤는지 남습니다. 잘못 저장하셨다면 GitHub 저장소의 커밋 목록에서 되돌릴 수 있습니다.

---

## 알아두실 점

- **저장 = 커밋 + 배포**입니다. 저장을 누르면 곧바로 실제 사이트가 바뀝니다. 1~2분 걸립니다.
- 저장하지 않고 다른 탭으로 가거나 창을 닫으려 하면 경고가 뜹니다.
- 같은 파일을 두 사람이 동시에 고치면, 나중에 저장하는 쪽에 "그 사이 파일이 변경되었습니다" 안내가 뜨고 덮어쓰기를 막습니다.
- 로그인은 8시간 뒤 자동으로 풀립니다.
- 비밀번호와 GitHub 토큰은 `admin.html` 안에 없습니다. Netlify 서버에만 있습니다.
- 상품을 처음 저장하면 `products.js` 의 줄바꿈 모양이 정리되면서 변경 줄이 많아 보일 수 있습니다. 내용은 그대로입니다.

---

## 안 될 때

| 증상 | 확인할 것 |
|---|---|
| `환경변수 ○○ 가 설정되지 않았습니다` | 3번 표에서 빠진 항목이 있는지, 넣은 뒤 재배포했는지 |
| `비밀번호가 맞지 않습니다` | `ADMIN_PASSWORD` 값 확인 (앞뒤 공백 주의) |
| `Bad credentials` / `Not Found` | GitHub 토큰이 만료되었거나 저장소·권한 설정이 잘못됨 |
| 저장은 됐는데 사이트가 그대로 | Netlify **Deploys** 탭에서 배포가 돌고 있는지 확인, 2분 뒤 새로고침 |

---

## 저장이 안 될 때 — 연결 진단

대쉬보드 **배포 · 이력** 탭 맨 아래 **연결 진단** → `진단하기` 를 누르면
토큰이 어디서 막혔는지 한 줄씩 알려 줍니다. 토큰 값 자체는 표시되지 않습니다.

| 나오는 줄 | 뜻 |
|---|---|
| `앞뒤공백` / `줄바꿈포함` 이 **있음** | 붙여넣을 때 공백이 딸려 왔습니다. Netlify 에서 값을 지우고 다시 붙여 넣으세요 |
| `토큰종류` 가 **알 수 없음** | `github_pat_` 이나 `ghp_` 로 시작하지 않습니다. 토큰이 아닌 값(이름 등)이 들어갔습니다 |
| `토큰확인` **실패 (401)** | 토큰이 만료됐거나 삭제됐습니다. GitHub 에서 새로 발급하세요 |
| `저장소확인` **실패 (404)** | 토큰의 Repository access 에 이 저장소가 빠져 있습니다 |
| `쓰기권한` **없음** | 토큰 권한의 Contents 를 **Read and write** 로 바꾸세요 |
| `축구키확인` **실패 (400)** | 경기 일정용 football-data.org 키가 무효입니다. 해당 사이트에서 키를 다시 확인해 넣으세요 |

> `GITHUB_TOKEN` 은 경기 일정 자동 갱신(`update-matches`)도 함께 씁니다.
> 이 값이 잘못되면 경기 일정도 갱신되지 않으니, 두 기능이 같이 멈췄다면 토큰부터 확인하세요.

### 환경변수를 만질 때 주의

여러 변수를 한 번에 고치다 보면 값이 섞이기 쉽습니다. **한 번에 하나씩** 고치고,
고칠 때마다 재배포한 뒤 진단으로 확인하세요. Scopes 는 **All scopes** 로 두시는 편이
단순하고 안전합니다 (보안상 차이는 크지 않습니다).
