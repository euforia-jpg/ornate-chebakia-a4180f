# 유포리아투어 관리자 대쉬보드 — 설치 안내

한 번만 설정하면 그 뒤로는 **대쉬보드에서 고치고 저장 버튼만 누르면** 사이트가 바뀝니다.
GitHub에 파일을 올리실 필요가 없어집니다.

---

## 1. 올릴 파일

압축을 풀면 아래 구조입니다. 그대로 저장소에 올려 주세요.

```
admin.html                          ← 대쉬보드 (새 파일)
concert.html                        ← 공연 목록이 보이도록 수정됨
data/concerts.json                  ← 공연 일정 (새 파일, 빈 목록)
data/fares.json                     ← 교통 요금 (새 파일, 빈 목록)
data/costsheets.json                ← 원가표 (새 파일, 빈 목록)
netlify/functions/admin-api.js      ← 관리자 백엔드 (새 파일)
netlify/functions/threads-api.js    ← 쓰레드 광고 전용 백엔드 (새 파일)
netlify/functions/matches-now.js    ← 경기 "지금 갱신하기" 창구 (새 파일)
netlify/functions/threads-connect.js ← 쓰레드 토큰 받아 오기 (새 파일)
netlify/functions/threads-callback.js ← 쓰레드 제거·삭제 콜백 (새 파일)
netlify/functions/threads-auto.js   ← 요일별 자동 광고 (새 파일)
netlify/functions/threads-cron.js   ← 자동 광고를 시간 맞춰 깨움 (새 파일)
_redirects                          ← 짧은 주소 연결 (새 파일)
netlify.toml                        ← 예약 실행 설정 (기존 파일에 한 줄 추가)
```

`netlify/functions/` 폴더가 저장소에 이미 있으니 그 안에 `admin-api.js` 와 `threads-api.js` 를 넣으시면 됩니다.
경기 갱신에 쓰는 `update-matches.js` 는 **이미 저장소에 있는 파일**이라 새로 올리실 것이 없습니다.

> ⚠️ **`data/` 안의 세 파일은 빈 목록(`[]`)입니다.**
> 저장소에 이미 같은 이름의 파일이 있고 **내용이 들어 있다면 덮어쓰지 마세요.**
> 처음 설치할 때만 올리시고, 다시 올리실 때는 `admin.html` 과 `netlify/functions/` 만 올리시면 됩니다.

---

## 2. GitHub 토큰 만들기 (한 번만)

대쉬보드가 저장소에 커밋하려면 열쇠가 필요합니다.

1. GitHub 오른쪽 위 프로필 → **Settings**
2. 맨 아래 **Developer settings** → **Personal access tokens** → **Fine-grained tokens**
3. **Generate new token** 을 누르고 이렇게 설정합니다
   - **Token name** — `euforia-admin`
   - **Expiration** — 1년 (만료되면 다시 만들어야 합니다)
   - **Repository access** — *Only select repositories* → `ornate-chebakia-a4180f` 하나만
   - **Permissions** → *Repository permissions* → **Contents** 를 **Read and write** 로
4. **Generate token** → 화면에 나오는 긴 문자열을 복사합니다

> 이 문자열은 **그 화면을 벗어나면 다시 볼 수 없습니다.** 바로 다음 단계에 붙여 넣으세요.
> 저에게도, 다른 누구에게도 보내지 마세요.

---

## 3. Netlify 환경변수 넣기 (한 번만)

Netlify → 해당 사이트 → **Site configuration** → **Environment variables** → **Add a variable**

| 이름 | 값 |
|---|---|
| `ADMIN_PASSWORD` | 대쉬보드에 쓰실 비밀번호 (직접 정하세요) |
| `ADMIN_SECRET` | 아무 긴 문자열 (아래 참고) |
| `GITHUB_TOKEN` | 2단계에서 복사한 토큰 |
| `GITHUB_REPO` | `euforia-jpg/ornate-chebakia-a4180f` |
| `GITHUB_BRANCH` | `main` |
| `NETLIFY_BUILD_HOOK` | (선택) 아래 4번 참고 |

**ADMIN_SECRET 만드는 법** — 로그인 상태를 확인하는 데 쓰는 값이라 아무도 못 맞출 문자열이면 됩니다.
키보드를 마구 눌러 40자 정도 만드셔도 되고, 아래를 그대로 쓰셔도 됩니다(한 번 정하면 바꾸지 마세요).

```
euf-2026-x7Kq9mZp3Rt8vNbW5jHs2Lc4Yd6Fg1Aa
```

넣고 나면 **Deploys → Trigger deploy → Deploy site** 로 한 번 배포해야 환경변수가 적용됩니다.

---

## 4. 재배포 버튼 (선택)

대쉬보드의 "지금 다시 배포하기" 버튼을 쓰시려면 빌드 훅이 필요합니다.
없어도 저장은 정상 작동합니다 (저장하면 자동으로 배포됩니다).

Netlify → **Site configuration** → **Build & deploy** → **Build hooks** → **Add build hook**
이름은 `admin-manual`, 브랜치는 `main` → 생성된 주소를 `NETLIFY_BUILD_HOOK` 에 넣으세요.

---

## 5. 접속

```
https://toureplus.co.kr/admin
```

메뉴에 없는 주소입니다. 검색엔진에도 잡히지 않게 막아 두었습니다.
북마크해 두고 쓰시면 됩니다.

---

## 쓰는 법

### 여행상품
상품을 누르면 편집 화면이 열립니다. 요금·기간·태그·포함/불포함·상세 일정표까지 고칠 수 있습니다.
**한 줄에 하나씩** 이라고 적힌 칸(태그, 하이라이트, 포함 사항 등)은 엔터로 줄을 나누면 됩니다.

### 공연 일정
`+ 공연 추가` 로 줄을 만들고 날짜·도시·공연장·공연명을 채웁니다.
**날짜가 지난 공연은 사이트에서 자동으로 사라집니다.** 지우실 필요 없습니다.
등록된 공연이 하나도 없으면 공연 페이지는 지금처럼 "준비중" 안내가 나옵니다.

### 교통 요금
MOVE 요금표입니다. 아직 요금을 안 정하셨으니 비워 두셔도 됩니다.

### 원가표 · 마진
단체별 원가를 넣으면 1인당 원가와 마진별 판매가가 바로 나옵니다.

- **상품을 고르면** 홈페이지에 올라간 판매가를 가져와 **현재 마진율**을 계산합니다
- **팀당 / 1인당** 구분이 핵심입니다
  - 팀당 — 인원과 상관없이 한 팀에 드는 비용 (차량 · 기사 · 가이드 · 로컬가이드 · 기사숙식 · 가이드숙식 · 가이드교통비)
  - 1인당 — 사람 수만큼 늘어나는 비용 (식사 · 입장료 · 호텔 · 시티택스)
- 수량은 **일수 · 박수를 바꾸면 자동으로 따라옵니다.** 직접 고친 칸은 그대로 둡니다
- 마진 계산은 두 가지 중에 고르실 수 있습니다
  - **매출 기준** (권장) — 판매가 = 원가 ÷ (1 − 마진). 정산할 때 쓰는 방식입니다
  - **원가 기준** — 판매가 = 원가 × (1 + 마진)
  - 같은 "30%"라도 결과가 다릅니다. 원가 100유로면 매출 기준은 142.9유로, 원가 기준은 130유로입니다
- **판매 경로와 커미션** — 광고 플랫폼처럼 판매가에서 수수료를 떼어 가는 경우를 반영합니다
  - 직판(0%) · 광고 플랫폼(20%) · 직접 입력 중에 고르시면 됩니다
  - 커미션은 **판매가 기준**으로 계산되고, 실수령액(판매가 − 커미션)에서 원가를 뺀 것이 순이익입니다
  - 목표 마진을 남기려면 받아야 할 판매가는 **원가 ÷ (1 − 커미션 − 마진)** 입니다.
    커미션 20%에서 마진 20%를 남기려면 원가의 2.5배를 받아야 합니다 (직판은 1.25배)
  - 커미션과 목표 마진을 합쳐 100%가 넘으면 "만들 수 없습니다"로 표시됩니다
- 마진이 **0% 아래면 적자 경고**, **10% 아래면 얇다는 경고**가 뜹니다
- 원가표는 단체명으로 저장해 두면 나중에 그대로 불러옵니다.
  저장 버튼을 눌러 목록에 담은 뒤 아래 **저장하고 배포**까지 눌러야 실제로 남습니다

### 쓰레드 광고
상품 · 공연 · 교통 · 경기 정보를 읽어 광고 초안을 만들고, 확인하신 뒤 버튼으로 올립니다.

- 초안은 **고쳐서 올리실 수 있습니다.** 발행 버튼을 눌러야 실제로 올라갑니다
- 쓰레드는 **500자까지**입니다. 글자수가 실시간으로 표시되고 넘으면 빨갛게 바뀝니다
- 올린 글은 대쉬보드에서 지울 수 없습니다. 쓰레드 앱에서 지우셔야 합니다
- “최근 올린 글”은 저장소가 아니라 **쓰레드 계정에서 직접** 가져옵니다

### 쓰레드 요일별 자동 발행
고르신 상품을 정해진 요일·시각에 **사진과 함께** 하나씩 돌아가며 올립니다.

**쓰는 순서**

1. **자동 발행 켜기** 체크
2. **요일**을 고릅니다 (기본 월·수·금) — 요일 버튼을 눌러 켜고 끕니다
3. **시각**을 고릅니다 — **스페인 현지 시각**입니다. 서머타임은 알아서 반영됩니다
4. 아래 목록에서 **광고에 넣을 상품**을 골라 **추가**
5. 상품마다 **사진 올리기** 로 사진을 넣습니다 (JPG·PNG·WEBP, 8MB 이하)
6. 문구를 고칩니다 — 상품 정보로 초안이 자동으로 채워져 있습니다. 500자까지
7. **설정 저장**

**버튼 세 개**

| 버튼 | 하는 일 |
|---|---|
| 설정 저장 | 설정을 저장합니다. 저장해야 실제로 적용됩니다 |
| 지금 미리보기 | 다음 차례가 어느 상품인지 **올리지 않고** 보여 줍니다 |
| 지금 한 번 올리기 | 요일·시각과 관계없이 **즉시 한 개를 올립니다** |

**알아두실 점**

- 사진이 없는 상품은 **차례를 건너뜁니다.** 쓰레드는 사진 있는 글의 반응이 훨씬 좋습니다
- **잠시 끄기** 를 누르면 목록에 남겨 둔 채 순서에서만 빼 둘 수 있습니다
- 순서는 날짜로 계산합니다. 기록을 따로 남기지 않아서 **GitHub 토큰이 만료돼도 자동 발행은 계속 돕니다**
- 올린 글은 대쉬보드에서 지울 수 없습니다. 쓰레드 앱에서 지우셔야 합니다

**사진은 어디에 저장되나요**

`images/threads/` 폴더에 들어가고, 사이트 주소로 공개됩니다.
쓰레드가 "인터넷에서 볼 수 있는 사진 주소"를 요구하기 때문입니다.
손님에게 보여 줄 사진이니 문제될 것은 없지만, **공개된다는 점은 알고 계세요.**

> **왜 함수가 두 개인가요?**
> `threads-cron.js` 가 매시 정각에 깨워 주고, 실제 일은 `threads-auto.js` 가 합니다.
> Netlify 는 예약 함수를 주소로 직접 부르면 막기 때문에, 나눠 두어야
> 대쉬보드에서 "지금 미리보기" 로 같은 코드를 시험해 볼 수 있습니다.

### 페이지 갱신
공연 페이지와 경기 페이지가 **지금 사이트에서 어떻게 보이는지**를 확인하고, 각각 따로 갱신하는 곳입니다.
두 페이지는 갱신되는 방식이 서로 다르기 때문에 버튼도 따로 두었습니다.

**공연 페이지** (`toureplus.co.kr/concert`)

| 보이는 것 | 뜻 |
|---|---|
| 등록된 공연 | `data/concerts.json` 에 들어 있는 전체 건수 |
| 예정된 공연 | 오늘 이후라서 손님에게 실제로 보이는 건수 |
| 지난 공연 | 날짜가 지나 페이지에서 자동으로 숨겨진 건수 |

- 공연은 **공연 일정 탭에서 저장하면 곧바로 반영**됩니다. 따로 갱신 버튼이 필요 없습니다
- **지금 확인** — 사이트에 올라가 있는 실제 파일을 읽어 옵니다. 저장이 잘 반영됐는지 볼 때 쓰세요
- **지난 공연 정리** — 날짜가 지난 공연을 목록에서 아예 빼 줍니다.
  화면에서만 빠지므로 **공연 일정 탭에서 “저장하고 배포”를 눌러야** 실제로 반영됩니다
- 상태 표시등: 예정된 공연이 없으면 빨강, 2건 이하면 주황, 그 외에는 초록입니다

**경기 페이지** (`toureplus.co.kr/matchday`)

| 보이는 것 | 뜻 |
|---|---|
| 등록된 경기 | `matchday.html` 에 들어 있는 전체 건수 |
| 남은 경기 | 오늘 이후 경기 건수 |
| 가장 가까운 경기 | 다음 경기 날짜 |

- 경기 일정은 football-data.org 에서 **정해진 시각에 자동으로** 받아 옵니다
- **지금 갱신하기** — 그 작업을 기다리지 않고 바로 돌립니다. 1~2분 뒤 사이트에 반영됩니다
- 갱신에는 **GITHUB_TOKEN 과 FOOTBALL_DATA_API_KEY 가 둘 다** 필요합니다.
  `Bad credentials` 가 보이면 GitHub 토큰, `API token is invalid` 가 보이면 축구 키 문제입니다
- `404` 가 나오면 저장소에 `netlify/functions/matches-now.js` 가 없거나 배포가 안 된 것입니다

> **왜 함수가 두 개인가요?**
> `update-matches.js` 는 `netlify.toml` 에 예약(schedule) 함수로 등록되어 있습니다.
> Netlify 는 예약 함수를 주소로 직접 부르면 **403 으로 막습니다.** 정해진 시각에만 돌리려는 규칙이에요.
> 그래서 `matches-now.js` 라는 얇은 창구를 하나 두고, 그것이 `update-matches` 의 코드를
> 대신 실행합니다. **갱신 로직은 여전히 `update-matches.js` 한 곳에만 있습니다.**
> `matches-now` 는 대쉬보드 로그인 토큰을 확인하므로 아무나 부를 수 없습니다.

> 이 탭의 “지금 확인”은 **사이트를 직접 읽습니다.** GitHub 토큰이 망가져 있어도 확인은 됩니다.
> 그래서 문제가 생겼을 때 어디까지 정상인지 가려내는 데 쓰기 좋습니다.

### 문의함
손님이 보낸 **예약문의 · 입금확인 · 경기예약**이 들어오는 대로 쌓입니다.

- 맨 위 띠가 **카카오톡·메일 알림 상태**를 알려 줍니다. 초록이면 정상, 주황이면 알림이 멈춘 것입니다
- 카카오톡이 막혀 있어도 **문의는 여기에 그대로 남습니다.** 주황 딱지(`알림 못 감`)가 붙은 건은 카카오톡도 메일도 못 간 것이니 먼저 확인해 주세요
- 초록 딱지(`메일로 감`)는 카카오톡은 못 갔지만 **메일로는 전해진** 건입니다
- 종류(예약문의 / 입금확인 / 경기예약)로 걸러 보실 수 있습니다
- 손님 이름·연락처가 들어 있는 화면이라 **로그인한 사람만** 볼 수 있습니다


**새 문의 알림**

대쉬보드를 열어 두시면, 새 문의가 들어오는 것을 **1~2분 안에** 알려 드립니다.

| 어디에 | 무엇이 보이나 |
| --- | --- |
| 위쪽 **문의함** 탭 | 빨간 숫자 (안 읽은 건수) |
| 브라우저 창 제목 | `(2) 관리자 — Euforia Tour` — 다른 탭을 보고 계셔도 눈에 띕니다 |
| 컴퓨터 알림창 | `새 문의 2건 / 김손님 · 북스페인 6일 외 1건` — 누르면 문의함으로 바로 갑니다 |
| 알림음 | 짧은 두 음. 알림 띠 오른쪽 **알림음** 체크로 끄고 켭니다 |

처음 문의함에 들어가시면 **알림 켜기** 단추가 있습니다. 한 번만 눌러 브라우저에 허용해 주시면 그다음부터는 계속 적용됩니다.
(실수로 막으셨다면 주소창 왼쪽 자물쇠를 눌러 "알림"을 **허용**으로 바꾸시면 됩니다.)

새로 들어온 건에는 **NEW** 딱지가 붙습니다. 문의함을 여시면 읽은 것으로 처리되고 빨간 숫자는 사라집니다.

> **어디까지 봤는지는 그 컴퓨터에만 적힙니다.** 사무실 컴퓨터와 집 컴퓨터가 따로 셉니다.
> **대쉬보드를 완전히 닫으면 알림창은 뜨지 않습니다.** 닫아 두셔도 받으시려면 아래 카카오톡이나 메일 알림을 쓰셔야 합니다.

**지우기**

각 건 오른쪽 **지우기** 를 누르면 바로 지워지지 않고 빨간 확인 줄이 펼쳐집니다.
**네, 지웁니다** 를 한 번 더 눌러야 실제로 지워집니다.

지운 직후에는 맨 위에 **되돌리기** 가 뜹니다.
다만 화면이 들고 있던 내용을 다시 써 넣는 방식이라, **그 화면을 벗어나거나 새로고침하면 되돌릴 수 없습니다.**
손님이 보낸 내용이니 지우실 때 한 번 더 확인해 주세요.

> **이 기능을 올리기 전에 들어온 문의는 남아 있지 않습니다.** 그때는 카카오톡으로만 보냈기 때문입니다.

---

## 카카오톡 알림이 안 올 때 — 다시 연결하기

카카오 리프레시 토큰은 **길어야 60일**이면 만료됩니다. 만료되면 알림만 멈추고, 문의는 문의함에 계속 쌓입니다.

문의함 맨 위 띠에 **"토큰 만료"** 라고 뜨면 아래대로 한 번만 다시 이어 주세요.

**1. 카카오 로그인 주소를 엽니다**

브라우저 주소창에 아래를 붙여 넣고 들어갑니다. (사장님 카카오 계정으로 로그인되어 있어야 합니다)

```
https://kauth.kakao.com/oauth/authorize?client_id=<KAKAO_REST_API_KEY>&redirect_uri=https%3A%2F%2Fornate-chebakia-a4180f.netlify.app%2F.netlify%2Ffunctions%2Fkakao-callback&response_type=code&scope=talk_message
```

`<KAKAO_REST_API_KEY>` 자리에는 Netlify 환경변수 `KAKAO_REST_API_KEY` 값을 그대로 넣으시면 됩니다.

**2. 동의하고 넘어갑니다**

"카카오톡 메시지 전송" 동의를 눌러 주세요. 이 권한이 있어야 **나에게 보내기**가 됩니다.

**3. 화면에 나온 값을 복사합니다**

`✅ 인증 성공!` 화면에 긴 문자열이 나옵니다. 칸을 누르면 전체 선택됩니다.

**4. Netlify 환경변수에 붙여넣습니다**

Netlify → Project configuration → Environment variables → **KAKAO_REFRESH_TOKEN** 을 새 값으로 바꿉니다.
저장하면 잠시 뒤부터 알림이 다시 옵니다. 문의함 새로고침으로 초록 띠가 되는지 확인해 보세요.

> **이 값은 비밀번호와 같습니다.** 채팅이나 메신저에 붙여넣지 마시고, Netlify 칸에만 넣어 주세요.

> **60일마다 반복됩니다.** 달력에 두 달에 한 번 알림을 걸어 두시면 편합니다.
> 잊으셔도 문의는 사라지지 않으니, 문의함만 가끔 열어 보시면 됩니다.

---

---

## 메일로 알림 받기 (한 번만 · 무료)

카카오톡은 **60일마다** 다시 연결해 주셔야 합니다. 깜빡하면 그동안 알림이 멈춥니다.
메일 알림을 같이 켜 두시면 **카카오가 멈춰 있어도 메일은 그대로 옵니다.** 만료가 없습니다.

Resend 라는 메일 발송 서비스를 씁니다. 하루 100통까지 무료이고, 저희는 하루 몇 통이면 충분합니다.

### 1. Resend 가입

1. <https://resend.com> → **Sign up**
2. **반드시 `euforia@euforiatour.com` 으로 가입해 주세요.** 이 주소로 메일이 오게 됩니다
3. 메일함에 온 확인 링크를 눌러 가입을 마칩니다

### 2. 열쇠(API Key) 만들기

1. 왼쪽 메뉴 **API Keys** → **Create API Key**
2. 이름은 아무거나 (예: `euforia-notify`), 권한은 **Sending access** 로 둡니다
3. **Add** 를 누르면 `re_` 로 시작하는 긴 글자가 한 번 뜹니다 — **이때 복사해 두세요.** 다시는 안 보여 줍니다

### 3. Netlify 에 넣기

Netlify → 사이트 → **Site configuration → Environment variables → Add a variable**

| 이름 | 값 |
| --- | --- |
| `RESEND_API_KEY` | 방금 복사한 `re_...` |
| `NOTIFY_EMAIL` | `euforia@euforiatour.com` (생략하면 이 주소가 기본값입니다) |

넣으신 뒤 **Deploys → Trigger deploy → Deploy site** 를 한 번 눌러 주세요. 새 값은 다음 배포부터 적용됩니다.

### 4. 확인

대쉬보드 **문의함** 을 열어 보시면 띠에 **메일 알림 — 켜짐** 이라고 뜹니다.
홈페이지에서 예약문의를 한 번 넣어 보시면 메일이 한 통 옵니다.

**보내는 사람이 `onboarding@resend.dev` 로 표시되는 것이 정상입니다.** 도메인 인증을 하지 않아도 사장님 본인 주소로는 보낼 수 있게 열어 둔 주소입니다.
예약문의 메일은 **답장을 누르면 바로 손님에게** 갑니다 (손님이 이메일을 적으신 경우).

> 회사 도메인(`@euforiatour.com`)으로 보내고 싶으시면 Resend 에서 도메인 인증(DNS 3줄)을 하신 뒤
> `NOTIFY_FROM` 에 `유포리아투어 <noreply@euforiatour.com>` 처럼 넣어 주시면 됩니다. 지금 당장은 안 하셔도 됩니다.

### 안 켜도 괜찮습니다

`RESEND_API_KEY` 를 넣지 않으시면 메일 부분은 **아무 일도 하지 않고 조용히 넘어갑니다.**
문의 접수도, 카카오톡 알림도, 문의함도 지금과 똑같이 돌아갑니다.

### 방문 통계
홈페이지를 몇 명이 보고 어떤 단추를 눌렀는지, 쓰레드에 올린 글이 몇 번 노출됐는지를 한 화면에서 봅니다.
위쪽 **기간**(7일 / 30일 / 90일)이 아래 내용 전체에 적용됩니다.

**홈페이지 방문·클릭**

| 보이는 것 | 뜻 |
|---|---|
| 페이지 조회 | 페이지가 열린 총 횟수 |
| 방문 | 사람 수. 브라우저 탭 하나를 한 번으로 셉니다 |
| 최근 7일 | 바로 앞 7일과 견준 증감이 아래에 함께 나옵니다 |
| 많이 본 페이지 · 상품 | 어떤 상품이 실제로 관심을 받는지 |
| 눌린 단추 | 예약문의·전화·카카오톡 등 어떤 행동으로 이어졌는지 |
| 어디서 들어왔나 | 네이버·구글·SNS·직접 입력 |
| 어느 나라에서 | 대한민국·스페인·일본 … 방문 수 기준 |
| 어느 지역에서 | 대한민국·서울, 스페인·마드리드 처럼 시·도 단위 |

- 그래프에 마우스를 올리면 그 날 숫자가 나옵니다. **표로 보기**를 누르면 최근 14일을 숫자로도 볼 수 있습니다
- **오늘 칸은 늘 낮습니다.** 하루가 아직 안 끝났기 때문이지, 방문이 줄어든 것이 아닙니다
- 기록은 **스페인 현지 날짜** 기준으로 하루를 묶습니다 (다른 자동 기능과 같은 기준)

**나라·지역은 어떻게 아나요 — 아이피를 저장하지 않습니다**

Netlify 가 요청을 받을 때 "이 사람은 한국 서울에서 왔다" 정도를 함께 알려 줍니다.
우리는 그 **이름만** 받아서 `대한민국 627` 처럼 숫자를 더합니다.

- 아이피 주소는 **읽지도, 저장하지도 않습니다**
- 누가 어디서 왔는지는 어디에도 남지 않습니다. 남는 것은 지역별 **합계 숫자**뿐입니다
- 그래서 "이 회사가 우리 사이트를 봤다" 같은 것은 알 수 없습니다.
  그걸 알려면 아이피를 저장해야 하는데, 그건 성격이 다른 이야기라 하지 않았습니다
- 시·도 단위까지만 셉니다 (구·동까지는 세지 않습니다)

지역 이름은 Netlify 가 영어로 주기 때문에, 한국·스페인의 자주 나오는 곳은 우리말로 바꿔 두었습니다.
그 밖의 지역은 영어 그대로 보입니다 (예: `일본 · Tokyo`).

> 만약 화면에 **"나라·지역 정보가 오지 않고 있습니다"** 라고 뜨면
> Netlify 가 이 방식의 함수에 위치를 안 넘겨 주는 경우입니다.
> 그 아래 줄에 나오는 목록을 그대로 알려 주시면 다른 방법으로 바꿔 드리겠습니다.

**남기는 것과 남기지 않는 것**

> 남기는 것 — 날짜, 어느 페이지, 어느 단추, 어느 검색엔진에서 들어왔는지, 나라·시도 이름
> **남기지 않는 것 — 방문자 아이피, 쿠키, 이름, 그 밖에 사람을 특정할 수 있는 어떤 것도**
>
> 쿠키를 쓰지 않으므로 쿠키 동의 배너가 필요 없습니다.
> 대신 "같은 사람이 내일 또 왔는지"는 알 수 없습니다. 방문은 **탭 기준**으로만 셉니다.

**쓰레드 글 성과**

쓰레드가 공식으로 알려 주는 숫자를 그대로 가져옵니다. 최근 12개 글의 노출·좋아요·댓글이 나옵니다.

- 숫자가 전부 `—` 로 나오고 "권한이 아직 없습니다" 라고 보이면
  → **쓰레드 광고 탭 → 쓰레드 연결하기**를 한 번 더 하시면 됩니다 (아래 설명 참고)
- 글 목록 자체는 권한이 없어도 늘 보입니다

---

## 방문 통계 켜기 (한 번만)

세 가지를 하시면 됩니다.

### 1. 파일 올리기

```
package.json                          ← 새 파일 (저장소 맨 위)
stats.js                              ← 새 파일 (저장소 맨 위)
netlify/functions/hit.js              ← 새 파일
netlify/functions/site-stats.js       ← 새 파일
netlify/functions/threads-stats.js    ← 새 파일
netlify/functions/inbox.js            ← 새 파일 (문의함)
netlify/functions/send-inquiry.js     ← 덮어쓰기 (문의를 먼저 저장하도록)
netlify/functions/send-deposit.js     ← 덮어쓰기
netlify/functions/send-matchday-booking.js ← 덮어쓰기
contact.html · deposit.html · matchday.html ← 덮어쓰기 (실패를 제대로 알리도록)
admin.html                            ← 덮어쓰기 (방문 통계 + 문의함 탭)
netlify/functions/threads-connect.js  ← 덮어쓰기 (성과 읽기 권한을 하나 더 요청합니다)
```

> `package.json` 은 Netlify 에게 "저장소 기능(Blobs)을 쓰는 꾸러미를 깔아 달라"고 알려 주는 파일입니다.
> 이것을 올린 뒤 첫 배포는 평소보다 30초쯤 더 걸립니다. 그 다음부터는 똑같습니다.
> 혹시 배포가 실패해도 **사이트는 직전 배포 그대로 계속 살아 있습니다.**

### 2. 홈페이지에 세기용 조각 붙이기 — 한 줄이면 끝납니다

홈페이지 파일들(`index.html`, `tours.html` …)을 하나하나 고칠 필요가 없습니다.
Netlify 가 모든 페이지에 자동으로 넣어 주는 기능이 있습니다.

1. Netlify → 프로젝트 → **Project configuration**
2. 왼쪽 **Build & deploy**
3. 맨 아래쪽 **Post processing** → **Snippet injection** → **Add snippet**
4. 이렇게 넣습니다

   - **Snippet name** — `방문 통계`
   - **Insert before** — `</body>` 를 고릅니다
   - 내용

     ```html
     <script src="/stats.js" defer></script>
     ```

5. **Save**

이렇게 하면 앞으로 새로 만드는 페이지에도 자동으로 붙습니다.
끄고 싶으실 때는 이 스니펫만 지우시면 됩니다. 홈페이지 파일은 건드리지 않았으니 아무 흔적도 남지 않습니다.

### 3. 쓰레드 성과 권한 받기 (쓰레드 숫자를 보려면)

지금 쓰는 쓰레드 열쇠에는 "글이 몇 번 노출됐는지 읽을 권한"이 없습니다. 한 번만 더 연결하시면 됩니다.

1. 메타 개발자 앱 → **Threads API 사용 사례 → 설정** 에서 `threads_manage_insights` 가 켜져 있는지 확인
2. 대쉬보드 → **쓰레드 광고** 탭 → **쓰레드 연결하기**
3. 브라우저가 **es.ronda 로 로그인되어 있는지 반드시 먼저 확인**하세요
4. 나오는 값 세 개를 Netlify 환경변수에 그대로 덮어씁니다

> `threads_manage_insights` 권한이 아직 준비되지 않았다면 연결 화면에서 거절당할 수 있습니다.
> 그때는 `netlify/functions/threads-connect.js` 안의
> `const SCOPES = ...` 줄에서 맨 뒤 `,threads_manage_insights` 만 지우고 다시 올리시면
> 예전처럼 발행만 되는 상태로 돌아갑니다. 발행 기능에는 아무 영향이 없습니다.

### 언제부터 숫자가 보이나요

스니펫을 저장한 **그 순간부터** 쌓입니다. 지난 방문은 알 수 없습니다.
하루 이틀 지나야 그래프 모양이 제대로 보입니다.

### 이 기능도 GitHub 토큰을 쓰지 않습니다

방문 기록은 저장소가 아니라 **Netlify 가 주는 저장 공간**에 쌓입니다.
그래서 GitHub 토큰이 만료돼도 통계는 계속 쌓이고, 통계 때문에 커밋이 늘어나는 일도 없습니다.

---

### 배포 · 이력
누가 언제 무엇을 고쳤는지 남습니다. 잘못 저장하셨다면 GitHub 저장소의 커밋 목록에서 되돌릴 수 있습니다.

---

## 쓰레드 광고 설정 (선택 · 다른 기능과 완전 분리)

이 기능은 **GitHub 토큰도 축구 키도 쓰지 않습니다.** 설정하지 않으셔도 나머지 탭은 다 됩니다.
반대로 GitHub 토큰이 만료돼도 쓰레드 발행은 계속 됩니다.

### 1. 메타 개발자 앱 만들기

1. [developers.facebook.com](https://developers.facebook.com) 로그인 → **My Apps → Create App**
2. 용도에서 **Threads API** 를 고릅니다
3. 앱 설정에서 쓰레드 계정 **@es.ronda** 를 연결합니다
4. 권한(scope)은 **`threads_basic`** 과 **`threads_content_publish`** 두 개면 됩니다

> **앱 심사는 지금 필요 없습니다.** 심사 기간에도 본인 계정에는 올릴 수 있습니다.
> 남의 계정에 대신 올려 주는 서비스를 만들 때만 심사가 필요합니다.

### 2. 장기 토큰 받기

처음 받는 토큰은 짧게 살고, 이를 교환하면 **60일짜리 장기 토큰**이 됩니다.
개발자 콘솔의 토큰 도구에서 교환하시면 됩니다.

### 3. Netlify 환경변수 (전부 `THREADS_` 로 시작)

| 이름 | 값 |
|---|---|
| `THREADS_USER_ID` | 쓰레드 사용자 ID (숫자) |
| `THREADS_TOKEN` | 60일짜리 장기 토큰 |
| `THREADS_TOKEN_ISSUED` | 토큰 받은 날짜 `2026-09-13` 형식 (남은 기간 계산용) |
| `THREADS_ACCOUNT` | 올려야 할 계정 이름 (예: `es.ronda`) — **안전장치** |
| `THREADS_APP_ID` | 쓰레드 앱 ID |
| `THREADS_APP_SECRET` | 쓰레드 앱 시크릿 코드 |

> `THREADS_ACCOUNT` 를 넣어 두면 **다른 계정 토큰으로는 글이 올라가지 않습니다.**
> 쓰레드 계정을 여러 개 쓰실 때 엉뚱한 계정에 광고가 올라가는 사고를 막아 줍니다.

넣고 재배포한 뒤 대쉬보드 **쓰레드 광고 → 연결 확인**을 눌러 주세요.
`@계정명`과 **남은 일수**가 뜨면 끝입니다.

### 60일마다 해야 할 일

토큰은 60일이면 만료됩니다. 남은 기간이 **14일 아래로 내려가면 경고**가 뜹니다.
새 토큰을 발급해 `THREADS_TOKEN` 을 바꾸고, `THREADS_TOKEN_ISSUED` 도 그날 날짜로 고쳐 주세요.

> 토큰이 만료돼도 **쓰레드 탭만 멈춥니다.** 사이트도, 상품 수정도, 원가표도 그대로 돌아갑니다.

---

## 알아두실 점

- **저장 = 커밋 + 배포**입니다. 저장을 누르면 곧바로 실제 사이트가 바뀝니다. 1~2분 걸립니다.
- 저장하지 않고 다른 탭으로 가거나 창을 닫으려 하면 경고가 뜹니다.
- 같은 파일을 두 사람이 동시에 고치면, 나중에 저장하는 쪽에 "그 사이 파일이 변경되었습니다" 안내가 뜨고 덮어쓰기를 막습니다.
- 로그인은 8시간 뒤 자동으로 풀립니다.
- 비밀번호와 GitHub 토큰은 `admin.html` 안에 없습니다. Netlify 서버에만 있습니다.
- 상품을 처음 저장하면 `products.js` 의 줄바꿈 모양이 정리되면서 변경 줄이 많아 보일 수 있습니다. 내용은 그대로입니다.

---

## 안 될 때

| 증상 | 확인할 것 |
|---|---|
| `환경변수 ○○ 가 설정되지 않았습니다` | 3번 표에서 빠진 항목이 있는지, 넣은 뒤 재배포했는지 |
| `비밀번호가 맞지 않습니다` | `ADMIN_PASSWORD` 값 확인 (앞뒤 공백 주의) |
| `Bad credentials` / `Not Found` | GitHub 토큰이 만료되었거나 저장소·권한 설정이 잘못됨 |
| 저장은 됐는데 사이트가 그대로 | Netlify **Deploys** 탭에서 배포가 돌고 있는지 확인, 2분 뒤 새로고침 |

---

## 저장이 안 될 때 — 연결 진단

대쉬보드 **배포 · 이력** 탭 맨 아래 **연결 진단** → `진단하기` 를 누르면
토큰이 어디서 막혔는지 한 줄씩 알려 줍니다. 토큰 값 자체는 표시되지 않습니다.

| 나오는 줄 | 뜻 |
|---|---|
| `앞뒤공백` / `줄바꿈포함` 이 **있음** | 붙여넣을 때 공백이 딸려 왔습니다. Netlify 에서 값을 지우고 다시 붙여 넣으세요 |
| `토큰종류` 가 **알 수 없음** | `github_pat_` 이나 `ghp_` 로 시작하지 않습니다. 토큰이 아닌 값(이름 등)이 들어갔습니다 |
| `토큰확인` **실패 (401)** | 토큰이 만료됐거나 삭제됐습니다. GitHub 에서 새로 발급하세요 |
| `저장소확인` **실패 (404)** | 토큰의 Repository access 에 이 저장소가 빠져 있습니다 |
| `쓰기권한` **없음** | 토큰 권한의 Contents 를 **Read and write** 로 바꾸세요 |
| `축구키확인` **실패 (400)** | 경기 일정용 football-data.org 키가 무효입니다. 해당 사이트에서 키를 다시 확인해 넣으세요 |

> `GITHUB_TOKEN` 은 경기 일정 자동 갱신(`update-matches`)도 함께 씁니다.
> 이 값이 잘못되면 경기 일정도 갱신되지 않으니, 두 기능이 같이 멈췄다면 토큰부터 확인하세요.

### 환경변수를 만질 때 주의

여러 변수를 한 번에 고치다 보면 값이 섞이기 쉽습니다. **한 번에 하나씩** 고치고,
고칠 때마다 재배포한 뒤 진단으로 확인하세요. Scopes 는 **All scopes** 로 두시는 편이
단순하고 안전합니다 (보안상 차이는 크지 않습니다).
